spec:
  template:
    spec:
      containers:
      - name: loadbalancer
        volumeMounts:
        - mountPath: /opt/bitnami/nginx/conf/server_blocks/presto
          name: presto-conf
      volumes:
      - name: presto-conf
        configMap:
          name: presto-config